services:
  backend:
    build:
      context: ./backend
    ports:
      - '8000:8000'

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: /api/v1
    ports:
      - '8080:80'
    depends_on:
      - backend